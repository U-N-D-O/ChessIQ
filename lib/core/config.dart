/// Feature toggles for the application.
///
/// Set `kEnableRemoteScoreboard` to `false` to disable Firebase scoreboard and
/// remote leaderboard sync during local development or while the feature is
/// being stabilized.
const bool kEnableRemoteScoreboard = false;
